/**
 * Interface for objects that use a tolerance for floating-point numerical operations.
 *
 * @export
 * @interface ITolerance
 */
export interface ITolerance {
  /**
   * Tolerance to use in all calculations with floating precision numbers.
   *
   * @type {number}
   * @memberof ITolerance
   */
  Tolerance: number
}
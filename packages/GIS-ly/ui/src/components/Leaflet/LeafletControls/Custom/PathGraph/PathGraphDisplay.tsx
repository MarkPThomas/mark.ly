import styles from './PathGraphDisplay.module.css';

export function PathGraphDisplay() {
  return (
    <div className={styles.container}>
      Track Graph to be added in next version
    </div>
  );
}